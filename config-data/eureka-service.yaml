server:
  port: 8761
eureka:
  client:
    #false porque eureka no se registra asi mismo	
    fetch-registry: false
    register-with-eureka: false
    #for multiple-instances  
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/  
