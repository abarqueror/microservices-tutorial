#Ya no son necesarios porque introducimos el microservicio config-server
#server.port=8002
#spring.application.name=car-service
#name: car-service debe coincidir con el yaml creodo en config-data
#Se debe arrancar primero el config-server
spring:
  application:
    name: car-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
